# Questo � il file esercizio-7-6.py
# 7.6: Usa `numpy` per creare un array e calcolare la media.
import numpy as np
array = np.array([1, 2, 3, 4, 5])
media = np.mean(array)
print(f"La media dell'array è {media}")

